(ns clojure-snippets-app.router-test
  (:require [clojure.test :refer :all]
            [clojure-snippets-app.router :refer :all]))

(deftest router-start-test
  (testing "FIXME, I fail."
    (let [expected 10 ]
      (clojure-snippets-app.router/start-router)
      )
    )
  )